---
import type { Difficulty } from "../utils/interfaces";
import { GAME_TITLE, BOARD_SIZE } from "../utils/constants";
import Layout from "./Layout.astro";
import { Timer } from "../components/Timer";
import { Board } from "../components/Board";

interface Props {
  difficulty: Difficulty;
}

const { difficulty } = Astro.props
---

<Layout title=`${GAME_TITLE} | ${difficulty.LABEL}`>
    <Timer client:load slot="title" />
    <Board client:load dimension={BOARD_SIZE} minesNumber={difficulty.MINES} />
</Layout>
